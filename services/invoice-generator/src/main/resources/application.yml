server:
  port: 8083

spring:
  application:
    name: invoice-generator
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/metering_db
    username: postgres
    password: password
  sql:
    init:
      mode: always # Forces Spring Boot to initialize database schemas
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: ALWAYS # Auto-creates QRTZ_* tables in PostgreSQL
      platform: postgres
    properties:
      org.quartz.scheduler.instanceName: InvoiceScheduler
      org.quartz.scheduler.instanceId: AUTO
      org.quartz.jobStore.useProperties: false
      org.quartz.jobStore.tablePrefix: QRTZ_
      org.quartz.jobStore.isClustered: true # Enables cluster locking across microservice replicas
      org.quartz.jobStore.clusterCheckinInterval: 20000
      # CRITICAL: Forces Quartz to treat bytea columns as binary data, not OID longs
      org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/